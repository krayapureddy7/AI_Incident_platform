# Mini Agentic AI Platform - Core Dependencies (Python 3.11+)
#
# Every dependency listed here is imported by the platform. Retrieval (BM25 +
# dense projection + RRF), the knowledge graph, and persistence are implemented
# against the standard library, so no vector-database, embedding-model, or graph
# engine dependency is required. See TRADE_OFFS.md for the managed-service
# equivalents a production deployment would adopt.

# Orchestration
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0   # Durable workflow checkpoints (resumable Tier-3 holds)

# Tool serving (MCP)
fastmcp>=0.1.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.28.0

# Contracts and validation
pydantic>=2.6.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                        # Required by fastapi.testclient
